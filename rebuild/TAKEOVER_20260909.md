# Codex takeover — checkpoint 56f333e

**Subsequent Windows gate:** the user applied `38f9cfc` and ran the full core
suite with PostgreSQL: **300 passed, 5 failed, 1 skipped**. The original runtime
limitations below describe the earlier handoff, not a missing Windows run.
See [CLOSEOUT_20260909.md](CLOSEOUT_20260909.md) for the five-failure diagnosis,
the follow-up changes and the remaining gate.

The interrupted work was recovered from `56f333e003277ff56a0150cda738822c5aa0ff06` on `feat/rebuild-core`. This follow-up is **not accepted for production yet**. PostgreSQL integration tests remain unexecuted in this runtime. No deployment or production configuration change was performed.

## Corrections in this follow-up

- Propagate inference failure kind/reason through `ClassificationResult`. The interrupted implementation read fields that did not exist and raised `AttributeError`. A transient failure now returns waiting work; missing configuration remains distinguishable from a content-level refusal.
- Persist a content-level inference refusal for its model/policy so lifecycle does not reopen the same unanswered content every cycle.
- Filter unusable search candidates before deciding that the domain selector is sufficient. An incompatible raw result no longer prevents the organization-ID selector from finding a valid buyer. Full final employer, authority, territory and email gates remain in effect.
- Reserve an Apollo recovery probe at the chargeable operation, not at a free search that may return no candidates.
- Enforce Fantastic refusal state immediately before page requests, including new partitions; recovery planning does not itself consume the reservation. Receipt timestamps use the injected observation clock, allowing quota staleness checks to recover correctly.
- Validate and lock partition ownership before changing posting data. A late response retains its IDs, payload and receipt with `fenced=true`, but cannot replace newer facts or move the cursor. Error/stall writes also require the partition token.
- Reject expired delivery leases before sending or writing receipts. Commit the terminal delivery receipt and outbox state together.
- Carry queue ownership into identity, classification and opportunity business writes, not only the final queue transition. Validate the classification input snapshot and approval input under row locks before committing decisions.
- Bind classification results to their input content hash and employer. Preserve the hash linked to each opportunity so replaying unchanged evidence cannot consume another evidence epoch. Migration 003 requeues unbound old classifications for local revalidation, without repurchasing inventory.
- Add independent review tests and PostgreSQL regression scenarios; remove one vacuous `or True` assertion. Enable the isolated core CI job on feature-branch pushes and retain its JUnit result.

## Evidence actually obtained here

| Check | Result |
| --- | --- |
| Original independent review at a090a2b | 21 failures / 12 passes, documented in the original review package |
| Checkpoint 56f333e, with storage fixtures adapted to its new interfaces | 32 passes / 1 genuine failure: missing `ClassificationResult.unavailable_kind` |
| New broad-search fallback reproduction before the correction | 33 passes / 1 failure: a valid org-ID buyer was never reached |
| Independent suite after corrections | 34 passes |
| All core cases not requiring PostgreSQL, provider network blocked | 178 passes / 1 skip / 127 PostgreSQL-dependent cases deselected |
| Integrity manifest | 35 checked / 0 mismatch / 0 absent |
| Python compilation, undefined-name check and diff whitespace | Passed |
| Full PostgreSQL suite and v1→v3 migration | Pending, not claimed as passed |
| Full legacy suite | Not accepted: automatic approval review blocked it for possible live Apollo activity |
| Live provider behavior, approved-lead gain, capacity/day | Unmeasured |

The portable tests exercise the actual domain/provider/service decisions with explicit storage fixtures. They do not validate PostgreSQL transaction semantics, concurrency or migrations. The nine new PostgreSQL tests must pass along with the rest of `tests_core` in a suitable isolated environment. The synthetic classifier corpus still does not establish relevance/recall for title-excluded real inventory.

## Access blockers

Local git publication failed because this execution environment has no GitHub write credential. The connected GitHub contents write separately returned `403 Resource not accessible by integration`. Neither publication nor CI was completed. The attached bundle preserves the new commits for application on the user's authenticated checkout.

Embedded PostgreSQL cannot start under this runtime's root-only user mapping. Attempts to use a non-root UID are unsupported; PostgreSQL's root check was not bypassed. The full legacy suite was also stopped as a validation route after automatic review rejected it for a possible Apollo call; it was not retried through another route. Its partial progress is not a successful gate.

## Next gate

Apply the bundle to an isolated checkout that contains checkpoint 56f333e. Run the full core suite with an embedded, disposable PostgreSQL and simulated providers, with production credentials and `TGTC_TEST_DATABASE_URL` absent. The included verification script constructs a restricted child environment and uses `ci_no_network`; it never runs a pipeline cycle, migration against production, provider acquisition, enrollment, merge or deployment.

Any PostgreSQL failure remains a blocker and needs correction before acceptance. Publish only `feat/rebuild-core` after the full core gate passes. The additional CI trigger runs the existing tests; publishing the branch does not authorize a merge or deployment.

Still outstanding from the original handoff: independent labelled semantic evaluation, actual production integration contracts and campaign/policy confirmation, migration/rollback acceptance, and a separately authorized live acceptance with an explicit budget. Existing lifetime evidence-epoch and per-epoch match limits remain policy assumptions requiring confirmation; this follow-up does not turn them into a measured daily ceiling.

The historical 5,218 / 2,722 / 172 / 4,505 duplicate events remain unattributed without response-level IDs. None of the offline fixes proves how many historical leads they lost or how many live Approved leads will be recovered. Counts 361 / 520 / 5,511 are observations for their original UTC windows; 781 / 6,205 is not an established approval conversion. There is no evidence here for either a 111/day ceiling or a promise of 1,000 new Approved leads/day.
