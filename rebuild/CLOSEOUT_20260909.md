# Follow-up to the Windows PostgreSQL gate

Input: the user's complete console transcript in `Pegado text(20260909-054506).txt`.
Applied head: `38f9cfc9af76676332bfcf3d64c879eecfd7b45e`.
Observed result: **300 passed, 5 failed, 1 skipped in 43.16 seconds**, against
embedded PostgreSQL on Windows with simulated providers and `ci_no_network`.

This change addresses those five reported failures. It does not claim a new full
PostgreSQL pass, deployment, measured lead gain or production readiness.

## Diagnosis and corrections

| Failure | Cause established by the transcript and code | Correction and retained invariant |
| --- | --- | --- |
| Source-local failure test | The fixture injects HTTP 401 and expects the next feed to ignore the resulting shared `fantastic=unauthorized` state. This contradicts the existing provider recovery guard. | Check that the next feed makes zero requests before the probe interval, then recovers and preserves the first feed's cursor. Add a separate HTTP 404 case proving ordinary source request failures do not block the other feed. No availability guard is disabled. |
| Match count after reopening | Five candidates exist, but epoch 1 enriches only the first three. The old test expected epoch 2 to magically skip the two never enriched, whose verification status search does not provide. | Assert the exact six distinct candidate/epoch pairs: p-0..p-2 in epoch 1, p-3/p-4/p-new in epoch 2. No judged candidate is retried; the per-epoch cap stays three. |
| Pre-enrichment skip reason | **Production reporting defect:** the new search prefilter correctly rejects an unsuitable title without paying, but the empty-result branch labels it as exhausted paid candidate history. | Emit `no_usable_candidates_in_search` when new search candidates fail the precheck. Preserve separate reasons for empty provider results and previously evaluated candidates. The existing integration test still requires the same person's later suitable title to reach approval, so the skip cannot become a permanent blacklist. |
| Cross-source duplicate fixture | Both fake jobs are outside the requested `[now-5h, now-4h)` window: the first is exactly at the excluded upper bound, the second five minutes later. Zero acquired rows is correct. | Put both observations inside the window and retain all canonical linkage assertions. Add adjacent-window coverage proving the lower boundary is included and the upper boundary enters the next window exactly once. Production cursors and filters are unchanged. |
| Expired approval delivery test | The first channel's precheck already revokes the approval and atomically blocks both outbox entries. The second drain correctly returns no work, but the test expected it to claim a blocked item. | Assert both persisted entries are blocked, both transports receive zero requests, and the approval is revoked. Exercise both channel orders. Blocked entries stay unclaimable. |

The 401 scenario uses the rebuild's single shared Fantastic credential/state. It
does not establish that every real endpoint-specific scope error has global scope.
That provider distinction requires actual integration evidence.

## Evidence obtained for this follow-up

- New independent reporting regression on unchanged `38f9cfc`: **1 failed,
  2 passed**. The failing case reproduces the incorrect reporting reason; the
  controls establish that empty search and already-judged history already work.
- Same cases after the correction: **3 passed**; full independent suite:
  **37 passed**.
- All collected core tests without PostgreSQL dependencies, with provider network
  blocked: **181 passed, 1 skipped, 130 PostgreSQL-dependent cases deselected**.
- Diff whitespace and undefined-name checks pass. Existing unused-import/local
  warnings are not correctness failures and were not used to broaden the patch.

The four test corrections are justified above by their fixtures and production
contracts, not by reducing expected output or deleting safety checks. New tests
strengthen the observable requirements. The only runtime change is the accurate
closure reason for unusable, unjudged search candidates.

## Next gate

Apply the incremental bundle on `feat/rebuild-core` at `38f9cfc`, preserving all
earlier commits. The included script reuses the user's dedicated test venv and
runs `rebuild/verify_takeover.py`: the full core suite with a disposable local
PostgreSQL and simulated providers, after removing inherited application
credentials and database URLs from the test environment. Results are written
outside the repository. No push, PR merge, deployment or live provider call is
part of the script.

The new full PostgreSQL result remains pending. The runtime here still cannot
start embedded PostgreSQL under its supported user mapping. No alternate route
was attempted around that limitation, and the previously rejected legacy test
route was not retried.

The broader acceptance items in `HANDOFF.md` remain: actual integration contracts,
independent semantic labels, policy confirmation, production migration/rollback
acceptance and separately authorized live acceptance. These changes make no claim
about the historical duplicate counts or a sustainable number of Approved leads
per day.
